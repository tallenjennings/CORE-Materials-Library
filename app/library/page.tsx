import { Nav } from '@/components/nav';import { prisma } from '@/lib/db';import { BinCard } from '@/components/ui';import { parseBinSearch } from '@/lib/rules';
export default async function Library({searchParams}:{searchParams:Promise<{q?:string}>}){const q=(await searchParams).q?.trim();const n=q?parseBinSearch(q):null;const bins=await prisma.bin.findMany({where:q?{OR:[...(n?[{binNumber:n}]:[]),{name:{contains:q,mode:'insensitive'}},{contentsDescription:{contains:q,mode:'insensitive'}},{category:{name:{contains:q,mode:'insensitive'}}}]}:{active:true},orderBy:{binNumber:'asc'},take:100});return <><Nav/><main className="mx-auto max-w-6xl p-4 space-y-4"><h1 className="text-3xl font-bold">Library</h1><form className="flex gap-2"><input name="q" defaultValue={q} className="flex-1 rounded border p-3"/><button className="touch rounded bg-[#123c69] text-white px-4">Search</button></form><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{bins.map(b=><a key={b.id} href={`/bins/${b.binNumber}`}><BinCard bin={b}/></a>)}</div></main></>}
